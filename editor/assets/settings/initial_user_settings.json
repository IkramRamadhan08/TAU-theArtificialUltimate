// TAU settings
//
// TAU is a modified fork of Tau. Upstream Tau settings remain compatible.
//
// To see all default settings without changing your
// custom settings, run `tau: open default settings` from the
// command palette (cmd-shift-p / ctrl-shift-p)
{
  "ui_font_size": 14,
  "buffer_font_size": 14,
  "theme": {
    "mode": "system",
    "light": "One Light",
    "dark": "One Dark",
  },
}
