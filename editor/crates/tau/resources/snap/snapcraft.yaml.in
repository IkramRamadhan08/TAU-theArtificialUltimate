name: tau
title: TAU
base: core24
version: "$RELEASE_VERSION"
summary: Local-first agentic coding IDE
description: |
  TAU is a local-first, agentic coding IDE forked from Zed with built-in AI agent runtime.
  All AI features use your own LLM provider API keys. No cloud dependency. No telemetry.
grade: stable
confinement: classic
compression: lzo
website: https://github.com/IkramRamadhan08/TAU-theArtificialUltimate
source-code: https://github.com/IkramRamadhan08/TAU-theArtificialUltimate
issues: https://github.com/IkramRamadhan08/TAU-theArtificialUltimate/issues
contact: https://github.com/IkramRamadhan08/TAU-theArtificialUltimate/issues

parts:
  tau:
    plugin: dump
    source: "https://github.com/IkramRamadhan08/TAU-theArtificialUltimate/releases/download/v$RELEASE_VERSION/tau-x86_64-linux.tar.xz"

    organize:
      # These renames seem to not be necessary, but it's tidier.
      bin: usr/bin
      libexec: usr/libexec

    stage-packages:
      - libasound2t64
      # snapcraft has a lint that this is unused, but without it Tau exits with
      # "Missing Vulkan entry points: LibraryLoadFailure" in wgpu.
      - libvulkan1
      # snapcraft has a lint that this is unused, but without it Tau exits with
      # "NoWaylandLib" when run with Wayland.
      - libwayland-client0
      - libxcb1
      - libxkbcommon-x11-0
      - libxkbcommon0

    build-attributes:
      - enable-patchelf

    prime:
      # Omit unneeded files from the tarball
      - -lib
      - -licenses.md
      - -share

      # Omit unneeded files from stage-packages
      - -etc
      - -usr/share/doc
      - -usr/share/lintian
      - -usr/share/man

apps:
  tau:
    command: usr/bin/tau
    common-id: ai.tau.TAU
    environment:
      TAU_BUNDLE_TYPE: snap
